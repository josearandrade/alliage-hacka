services:
  app:
    build: .
    image: alliage-hacka:latest
    ports:
      - "${ALLIAGE_PORT:-7860}:7860"
    environment:
      GRADIO_SERVER_NAME: 0.0.0.0
      GRADIO_SERVER_PORT: 7860
    volumes:
      - alliage-hf-cache:/app/data/hf_cache

volumes:
  alliage-hf-cache:
